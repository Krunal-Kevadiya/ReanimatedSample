import React from 'react';
import { Images } from '../../theme';
import { CardFold } from '../../components/fold-card';

export default (): React.ReactElement => <CardFold front={Images.queenOfSpade} />;
